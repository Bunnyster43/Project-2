{{!-- clicked thread-link data-id = question.id --}} {{!-- ajax call to comments table for all comments with matching question
id joined to questions table entry with id value equal to data-id --}}

<h4>{{question}} {{#if isAnswered}} [ANSWERED] {{/if}} </h4>
{{!-- display question author --}}
<p>Asked by {{user_name}}</p>
<ul>
    {{#each comments}}
    <li>
        <p>{{content}} -by {{user_name}}</p>

    </li>
    {{/each}}
</ul>
<h2>Submit a Reply!</h2>
<div class="col-12">
    <textarea name="demo-message" id="demo-message" placeholder="Type your comment here" rows="6"></textarea>
</div>
<br>
<!-- Break -->
<div class="col-12">
    <ul class="actions">
        <li>
            <input type="submit" value="Submit" class="primary" question-id={{id}} />
        </li>
        <li>
            <input type="reset" value="Reset" />
        </li>
    </ul>
    <hr class="major" />
</div>